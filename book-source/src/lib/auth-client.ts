import { createAuthClient } from "better-auth/react";

export const authClient = createAuthClient({
  // Configuration for the client
  // In a Docusaurus app, we'll need to handle the API endpoint configuration
});